// Lets put tests here!
const db = require('../db.js');

var assert = require('assert');
describe('db.js', function() {
  describe('getCourses Function', function() {
    it('Shoud Return Rows Object if query is valid', function() {
      assert.ok(1)
    });
    it('Shoud Return error Object if query is invalid', function() {
      assert.ok(1)
    });
  });
});
